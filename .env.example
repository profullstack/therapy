# AI Provider (openai or ollama)
PROVIDER=openai

# OpenAI API Key (required if using OpenAI provider)
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Default model to use (if not specified via CLI)
# MODEL=gpt-4-turbo